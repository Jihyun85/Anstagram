extends ./templates/main.pug

block content
  .form-container
    h2 #{pageTitle}
    form(action=routes.upload, method="post")
      label(for="imageFile") 이미지 선택
      input(type="file", id="imageFile", required=true)
      textarea
      input(type="submit", value="업로드")

