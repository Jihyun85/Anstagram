extends ./templates/main.pug

block content
  .form-container
    h2 #{pageTitle}
    p 이미지는 수정이 불가능합니다.
    form(action=routes.editContent, method="post")
      textarea
      input(type="submit", value="업로드")
